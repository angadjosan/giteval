name: Deploy to Google App Engine

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'frontend/**'
      - 'shared/**'
      - '.github/workflows/deploy-app-engine.yml'      
  workflow_dispatch:

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  GAE_APPLICATION: ${{ secrets.GAE_APPLICATION }}

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest
    name: Deploy Frontend
    
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Build Next.js
        working-directory: ./frontend
        env:
          # NEXT_PUBLIC_API_URL is optional - defaults to api.giteval.com/api
          # Can be set via GitHub secret NEXT_PUBLIC_API_URL or Secret Manager secret giteval-next-public-api-url
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL || 'https://api.giteval.com/api' }}
        run: npm run build

      - name: Prepare standalone deployment
        working-directory: ./frontend
        run: |
          test -d ".next/standalone" || (echo "Error: Standalone build not found!" && exit 1)
          
          mkdir -p deploy
          cp -r .next/standalone/. deploy/
          mkdir -p deploy/.next
          cp -r .next/static deploy/.next/
          cp -r public deploy/
          cp app.yaml deploy/
          
          # Ensure start script matches entrypoint and skip App Engine build
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('deploy/package.json', 'utf8'));
            pkg.scripts = pkg.scripts || {};
            pkg.scripts.start = 'node server.js';
            pkg.scripts['gcp-build'] = ''; // Skip App Engine automatic build
            delete pkg.scripts.build;
            delete pkg.scripts.dev;
            fs.writeFileSync('deploy/package.json', JSON.stringify(pkg, null, 2));
          "
          
          test -f deploy/server.js || (echo "Error: server.js not found!" && exit 1)

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy Frontend to App Engine
        working-directory: ./frontend/deploy
        run: |
          # Create deploy-specific .gcloudignore
          cat > .gcloudignore << 'EOF'
          .git/
          .gitignore
          *.md
          .env*
          *.log
          .DS_Store
          EOF
          
          # Verify server.js exists
          test -f server.js || (echo "Error: server.js not found!" && exit 1)
          
          # Deploy (secrets are loaded from Secret Manager at runtime)
          VERSION_NAME="frontend-$(date +%Y%m%d-%H%M%S)"
          gcloud app deploy app.yaml \
            --project=$PROJECT_ID \
            --quiet \
            --version=$VERSION_NAME

  deploy-backend:
    runs-on: ubuntu-latest
    name: Deploy Backend
    needs: deploy-frontend  # Deploy backend after frontend is deployed

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install backend dependencies
        working-directory: ./backend
        run: npm ci

      - name: Build TypeScript
        working-directory: ./backend
        run: npm run build

      - name: Verify build output
        working-directory: ./backend
        run: |
          test -f dist/backend/src/index.js || (echo "Error: dist/backend/src/index.js not found!" && exit 1)
          echo "✓ Build verified"

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy Backend to App Engine
        working-directory: ./backend
        run: |
          # Deploy (secrets are loaded from Secret Manager at runtime)
          VERSION_NAME="backend-$(date +%Y%m%d-%H%M%S)"
          gcloud app deploy app.yaml \
            --project=$PROJECT_ID \
            --quiet \
            --version=$VERSION_NAME
