# This file specifies files that are *not* uploaded to Google Cloud
# using gcloud. It follows the same syntax as .gitignore

# Source files (not needed - we only deploy dist/)
src/
# tsconfig.json is needed for gcp-build if it runs, but we skip it with gcp-build script
# Keeping it excluded since we skip the build step

# Development files
.git/
.gitignore
.env*
*.log
*.tsbuildinfo
*.md
.DS_Store

# Dependencies (App Engine will install from package.json)
node_modules/
