// Type definitions for GitEval
export type Grade = 'A+' | 'A' | 'A-' | 'B+' | 'B' | 'B-' | 'C+' | 'C' | 'C-' | 'D' | 'F';

export interface Evaluation {
  // TODO: Add evaluation interface
}

export interface CategoryScore {
  // TODO: Add category score interface
}

export interface Suggestion {
  // TODO: Add suggestion interface
}

export interface Metrics {
  // TODO: Add metrics interface
}

export interface RepositoryMetadata {
  // TODO: Add repository metadata interface
}

export interface UserProfile {
  // TODO: Add user profile interface
}
